server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userservice_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Shan@2005
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

services:
  order-service:
    url: http://localhost:8083
  notification-service:
    url: http://localhost:8087

jwt:
  secret: MyJwtSecretKey12345
  expiration: 3600000

logging:
  level:
    root: INFO
    com.example.userservice: DEBUG

app:
  welcome-message: "Welcome"
  feature:
    registration-enabled: true
